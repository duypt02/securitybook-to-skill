skill:
  required_fields:
    - skill_name
    - objective
    - context
    - preconditions
    - procedure
    - tools_commands
    - expected_outputs
    - safety_constraints
    - references

  optional_fields:
    - inputs
    - decision_points
    - troubleshooting
    - reporting_notes
    - related_skills

# knowledge_types: classification taxonomy for concept chapter content.
# Defined here for documentation and harness guidance; not currently enforced
# by the evaluator. Use these labels when deciding which section of a chapter
# a piece of source knowledge belongs to.
knowledge_types:
  conceptual:
    description: "Definitions, principles, concepts, technical background"
  procedural:
    description: "Ordered steps, methodology, workflows"
  command:
    description: "Commands, parameters, scripts, configuration snippets"
  tool:
    description: "Tool usage, tool workflow, output interpretation"
  scenario:
    description: "Lab context, assumptions, preconditions, applicable situations"
  troubleshooting:
    description: "Common errors, failure cases, fixes"
  reporting:
    description: "Evidence, notes, findings, report guidance"
  safety:
    description: "Legal, ethical, authorized-use constraints"